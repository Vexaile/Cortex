# Multilang examples

Files here exercise Cortex's language support.

- `blink.zig` - Zig syntax highlighting
- `ring.c` - plain C, built with `gcc` and a **C** standard
- `firmware.elf` - a binary; Cortex refuses to open it as text

```c
int main(void) { return 0; }
```
